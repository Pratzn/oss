<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:jdbc="http://www.springframework.org/schema/jdbc"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xmlns:aop="http://www.springframework.org/schema/aop"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
        http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-3.2.xsd
        http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.2.xsd
        http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.2.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd">

    <!-- このXMLを利用する場合は、root-context.xmlのimportを有効にしてください。 -->

    <description>アプリケーションコンテキスト - データアクセス関連のXML定義</description>


    <!-- [データソースの設定] -->
    <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
        <property name="driverClassName" value="${jdbc.driverClassName}" />
        <property name="url" value="${jdbc.url}" />
        <property name="username" value="${jdbc.username}" />
        <property name="password" value="${jdbc.password}" />
        <property name="defaultAutoCommit" value="false" />
    </bean>

    <!-- [トランザクションマネージャの設定] -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource" />
    </bean>

    <!-- [宣言的トランザクションの設定] -->
    <!-- トランザクションのAOP設定 -->
    <aop:config>
        <aop:advisor pointcut="@within(org.springframework.stereotype.Service)"
                advice-ref="transactionAdvice" />
    </aop:config>

    <!-- トランザクションアドバイス -->
    <tx:advice id="transactionAdvice">
        <tx:attributes>
            <tx:method name="create*" read-only="false" rollback-for="java.lang.Exception" />
            <tx:method name="update*" read-only="false" rollback-for="java.lang.Exception" />
            <tx:method name="delete*" read-only="false" rollback-for="java.lang.Exception" />
            <tx:method name="read*" read-only="true" />
        </tx:attributes>
    </tx:advice>


    <!-- [アノテーションによるトランザクションの設定] -->
    <!-- @Transactionalアノテーションで、トランザクションをかけたい場合は、アプリケーション
         内で統一するため、宣言的トランザクションの設定を削除し、コメントをはずしてください。
    <tx:annotation-driven />
    -->


    <!-- [MyBatisの設定] -->
    <!-- SpringとMyBatisの連携用Bean設定 -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <property name="typeHandlers">
            <array>
                <bean class="jp.co.nssol.dukenavire.mybatis.type.DNDateTimeTypeHandler"/>
            </array>
        </property>
        <property name="configLocation" value="classpath:mybatis-mapper-config.xml" />
    </bean>

    <!-- Import of Mapper configuration file-->
    <!-- Mapper - Mapper project that generated the class configuration file (when creating the project
          Please import the is automatically generated).
    -->

    <import resource="classpath:noss-dao-context.xml" />
    <import resource="classpath:noss-logic-context.xml" />


</beans>
